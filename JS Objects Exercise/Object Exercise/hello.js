// This is what is given

// function hello(name) {
//   // should return "hello name"
//   return -1;
// }

// module.exports = hello;

//Answer to fix the code. 
function hello(name) {
  // should return "hello name"
  return "hello " + name;
}

module.exports = hello;
